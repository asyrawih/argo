apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: app-deployment
    managed-by: kustomize
data:
  ENVIRONMENT: base
  SAMPLE: "This is a sample value in the base configuration"
